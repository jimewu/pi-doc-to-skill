*.pyc
__pycache__/

# Agent session traces — never commit
.stone/
**/.stone/

# PyPI packaging build directories
dist/
build/
*.egg-info/

# uv lockfile (not pinned for a library/tool; deps live in pyproject extras)
uv.lock

# Repo-local virtualenv for crawl dependencies (setup-venv.sh)
.venv/

# Local design specifications & prompts with agent
PLAN

# MkDocs: build output
site/

# Crawl workspace / test artifacts
*.crawl_tmp/

# Node.js dependencies (extension dev; resolved at install time)
node_modules/

# Tool caches
.pytest_cache/
.ruff_cache/
